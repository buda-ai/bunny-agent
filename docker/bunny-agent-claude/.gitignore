# Build context (temporary files for Docker build)
.build-context/

# Generated Dockerfile (optional, can be regenerated)
# Dockerfile

# Environment variables
.env

# Patch file staged by CI from ../../patches/ before docker build
pi-coding-agent.patch
