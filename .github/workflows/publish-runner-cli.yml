name: Publish @sandagent/runner-cli to NPM

on:
  push:
    branches:
      - main
      - chore*
  workflow_dispatch:  # Manual trigger

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test --filter=@sandagent/runner-cli --filter=@sandagent/runner-claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Build
        run: |
          pnpm build --filter=@sandagent/runner-claude
          pnpm build --filter=@sandagent/runner-cli

      - name: Type check
        run: pnpm typecheck --filter=@sandagent/runner-cli --filter=@sandagent/runner-claude

      - name: Publish to NPM
        run: pnpm publish --access public --no-git-checks --provenance
        working-directory: ./apps/runner-cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}