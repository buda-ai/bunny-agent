name: Publish Selected Packages to NPM

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build dependencies
        run: |
          pnpm --filter @bunny-agent/runner-claude build
          pnpm --filter @bunny-agent/runner-codex build
          pnpm --filter @bunny-agent/runner-gemini build
          pnpm --filter @bunny-agent/runner-opencode build
          pnpm --filter @bunny-agent/runner-pi build

      - name: Verify npm auth
        run: npm whoami
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.NODE_AUTH_TOKEN || secrets.NPM_PUBLISH_TOKEN }}

      - name: Publish selected packages
        shell: bash
        run: |
          set -euo pipefail

          publish_if_needed() {
            local package_name="$1"
            local package_dir="$2"

            local local_version
            local_version="$(node -p "require('./${package_dir}/package.json').version")"

            echo "Checking ${package_name}@${local_version}"
            if npm view "${package_name}@${local_version}" version >/dev/null 2>&1; then
              echo "Skipping ${package_name}@${local_version} (already published)"
              return
            fi

            echo "Building ${package_name} and workspace dependencies"
            pnpm -r --filter "${package_name}..." build

            echo "Publishing ${package_name}@${local_version}"
            (
              cd "${package_dir}"
              pnpm publish --access public --no-git-checks
            )
          }

          publish_if_needed "@bunny-agent/sdk" "packages/sdk"
          publish_if_needed "@bunny-agent/manager" "packages/manager"
          publish_if_needed "@bunny-agent/sandbox-sandock" "packages/sandbox-sandock"
          publish_if_needed "@bunny-agent/runner-cli" "apps/runner-cli"
          publish_if_needed "@bunny-agent/daemon" "apps/daemon"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN || secrets.NODE_AUTH_TOKEN || secrets.NPM_PUBLISH_TOKEN }}
