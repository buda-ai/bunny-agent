{
  "name": "sandagent",
  "version": "0.1.0",
  "private": true,
  "description": "Sandboxed Super Agent runtime that speaks AI SDK UI natively",
  "repository": {
    "type": "git",
    "url": "https://github.com/vikadata/sandagent.git"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
