# Synchronize origin/main into runner alignment

Date: 2026-07-30

## What Changed

- Fetched the latest `origin` references and merged `origin/main` into the
  runner-alignment synchronization branch.
- Reconciled the example chat page so automatic compaction status remains
  visible while request-scoped MCP configuration is included in initial and
  follow-up chat requests.
- Restored the Pi runner option type import required by the merged harness MCP
  configuration contract.

## Verification

- `pnpm install --frozen-lockfile`
- Built runner declarations for ACP, Claude, Gemini, OpenCode, and Pi.
- Type checks passed for web, daemon, runner CLI, manager, ACP, Claude,
  Gemini, runner harness, OpenCode, Pi, and SDK.
- Web tests passed: 2 files and 14 tests.
- Daemon, runner CLI, manager, runner harness, Pi, and SDK tests passed: 39
  files and 420 tests.
- ACP, Claude, Gemini, OpenCode, and runner harness tests passed: 10 files and
  78 tests.
- Biome checks passed for the manually reconciled TypeScript files.
- `git diff --cached --check`
