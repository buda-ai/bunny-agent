# 2026-03-11 Release 0.7.2

## Session Log

- Started patch release workflow to bump fixed release packages from `0.7.1` to `0.7.2`.
- Confirmed release workflow now executes but npm publish currently fails for `@bunny-agent/sdk` and related packages with registry `E404`.
- Added changeset `.changeset/dull-schools-ring.md` with coordinated patch bumps for the fixed package group.
- Ran `pnpm run version` (`changeset version`) and applied `0.7.2` package/changelog updates.
- Fixed CI publish auth configuration: added fallback token chain (`NPM_TOKEN` -> `NODE_AUTH_TOKEN` -> `NPM_PUBLISH_TOKEN`) in release and runner-cli publish workflows.
- Added explicit `npm whoami` preflight checks to fail fast with clear authentication errors before publish.
- Validated workflow syntax with `actionlint`.
- Enabled `@bunny-agent/manager-cli` publishing by removing `private: true` and adding `publishConfig.access: public`.
- Added `@bunny-agent/manager-cli` to changesets fixed release group and removed it from the ignore list.
- Reworked npm publish workflow to publish selected packages on each `main` push: `@bunny-agent/sdk`, `@bunny-agent/runner-cli`, and `@bunny-agent/manager-cli`, publishing only when the version is not already on npm.
- Updated release workflow to stop direct npm publishing; publish responsibility is now centralized in the selected-packages workflow.
- Validated all workflow files with `actionlint`.
- Adjusted CI publish target set to exactly: `@bunny-agent/sdk`, `@bunny-agent/manager`, `@bunny-agent/sandbox-sandock`, `@bunny-agent/runner-cli`.
- Removed `@bunny-agent/manager-cli` from changesets fixed release group and re-added it to ignore list.
