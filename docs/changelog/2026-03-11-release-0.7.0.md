# 2026-03-11 Release 0.7.0

## Session Log

- Initialized release workflow for version `0.7.0`.
- Audited workspace status and changesets configuration.
- Confirmed fixed release group packages currently at `0.6.3` and ready for coordinated bump.
- Added release changeset `.changeset/clever-teachers-jump.md` with coordinated minor bumps for the fixed package set.
- Ran `pnpm run version` (`changeset version`) and applied release updates.
- Confirmed fixed release group package versions are now `0.7.0`.
- Prepared release commit artifacts for version `0.7.0`.
- Removed `publishConfig.tag: beta` from `@sandagent/runner-cli`, `@sandagent/manager`, and `@sandagent/sandbox-e2b` so npm publish defaults to `latest`.
