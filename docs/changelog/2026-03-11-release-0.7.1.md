# 2026-03-11 Release 0.7.1

## Session Log

- Started patch release workflow to bump fixed release packages from `0.7.0` to `0.7.1`.
- Verified clean working tree and current release versions.
- Added changeset `.changeset/nasty-mugs-enjoy.md` with coordinated patch bumps for the fixed package group.
- Ran `pnpm run version` (`changeset version`) and applied `0.7.1` package/changelog updates.
- Investigated missing `@bunny-agent/sdk@0.7.1` publish and confirmed release workflow failed before jobs started due to YAML parse error.
- Fixed `.github/workflows/release.yml` by replacing invalid inline expression values in `changesets/action` `title`/`commit` fields with valid quoted strings.
- Validated workflow syntax with `actionlint`.
- Fixed release workflow pnpm setup mismatch by removing hardcoded `version: 9` from `pnpm/action-setup` (repo uses `pnpm@10.11.0`).
